import styled from 'styled-components'

export const Container = styled.div`
  display: flex;
  flex-direction: row;
  align-items: center;

  & > p {
    color: #fff;
    text-align: left;
    margin-left: 8px;
  }
`
